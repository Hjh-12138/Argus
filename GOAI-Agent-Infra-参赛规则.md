# GOAI 新智基座 Agent Infra 参赛规则

> 来源：`6e21b053-f18b-4857-83e2-835bd96d5434.pdf` 及其 OCR 可编辑版 `6e21b053-f18b-4857-83e2-835bd96d5434-editable.docx`。
>
> 本文针对易发生 OCR 错误的日期、数量、必选项、评分比例和红线，重新对照了原 PDF 页面。手册中的赛程仅标注月日，具体年份及临时调整以赛事官网和组委会最终通知为准。

## 一、核心参赛结论

本赛题不是泛化的“任意多 Agent 项目”，而是有明确框架约束的 **Agent Infra 工程赛题**：

> 必须以 **AgentTeams（原名 Hiclaw）**作为多 Agent 协同设计基点，设计至少 **3 个不同职能的 Agent**，将关键能力封装为可复用 **Skill**，形成一个可运行、可验证、可审计、可回滚的端到端企业任务闭环。

如果项目存在以下情况，则不符合或难以充分满足赛题要求：

- 没有真正基于 AgentTeams 编排；
- 只是自己实现循环或状态机，然后把多个 Prompt 称为 Agent；
- AgentTeams 只出现在文档或适配器名称中；
- 没有可复用 Skill；
- 没有结果验证和执行证据；
- 没有审批、回滚和审计。

---

## 二、硬性门槛

### 2.1 至少三个不同职能的 Agent

作品必须包含：

- 不少于 **3 个不同职能 Agent**；
- 每个 Agent 都有清晰身份；
- 职责、输入、输出和能力边界必须明确；
- 多个 Agent 必须协作完成一个端到端闭环，不能只是各自独立对话。

必须提交 **Agent Identity 清单**，至少描述：

| 字段 | 要求 |
|---|---|
| Name | Agent 唯一标识 |
| Role | 职责定位 |
| Capabilities | 能做什么、不能做什么 |
| Inputs | 接收什么输入 |
| Outputs | 输出格式和质量要求 |
| Dependencies | 依赖哪些 Agent、Skill 或工具 |
| Decision Boundary | 自主决策边界，以及何时必须人工确认 |
| Trace | 执行过程如何记录、回放和审计 |

### 2.2 AgentTeams 是必选框架

规则明确要求：

> 多 Agent 设计必须以 AgentTeams 作为协同设计基点。

项目需要明确展示 AgentTeams 如何承担：

1. 角色编排；
2. 任务拆解；
3. 上下文传递；
4. 协同执行；
5. 状态追踪。

评审不是看代码或 PPT 中有没有出现 `AgentTeams` 这个名字，而是看以上机制是否真实映射到 AgentTeams 的框架能力。

因此：

- 做一个自研编排器，再在外围包装 AgentTeams adapter，存在较大风险；
- 只有模型调用，没有 AgentTeams 角色与任务结构，不满足要求；
- AgentTeams 不能只作为可选 provider；
- 核心执行链应由 AgentTeams 驱动，或至少提供可验证的 AgentTeams 工程材料。

### 2.3 Skill 是必选项

规则明确写明：

> Skill 是本赛题必选项。

Skill 必须是稳定、可复用的任务能力抽象，而不是：

- 一次性脚本；
- 写死在 Agent Prompt 中的行为描述；
- 单次 Demo 专用逻辑；
- 与特定输入强耦合的临时代码。

每个方案至少应提供一份核心 Skill 清单。每个 Skill 必须说明：

- 名称；
- 用途；
- 输入与输出；
- 调用条件；
- 依赖工具或系统；
- 失败处理；
- 权限与安全边界；
- 复用价值；
- 与多 Agent 协同流程的关系。

评审还会检查：

- Skill 是否能被多个 Agent 或多个场景复用；
- 是否有版本管理；
- 是否支持发布和回滚；
- 是否有质量评估；
- 是否考虑开源分发。

#### 阿里云官方用云 Skills

规则表格中将阿里云官方用云 Skills 标为“推荐”，FAQ 中则使用了“并使用阿里云官方用云 Skills”的表述。最稳妥的处理方式是：

- Skill 本身必选，可采用官方或自定义 Skill；
- 阿里云官方用云 Skills 属于强烈推荐；
- 若不使用官方 Skills，应说明替代原因、接口兼容性和迁移成本。

### 2.4 必须形成完整任务闭环

方案必须覆盖以下八个环节：

1. **任务输入**：接收告警、工单、日志、账单、安全事件或其他企业任务。
2. **任务拆解**：主控 Agent 或协同框架将任务拆给不同职能 Agent。
3. **上下文传递**：在 Agent 之间传递任务上下文、历史信息、工具结果和中间结论。
4. **工具调用**：Agent 通过 Skill、MCP、云产品、企业系统、知识库或外部接口执行任务。
5. **结果验证**：对修复、安全处置、成本优化或其他执行结果进行验证。
6. **执行证据沉淀**：保存日志、Trace、Metrics、报告、截图或其他证据。
7. **审批与回滚**：高风险动作必须支持人工确认、审批、回滚和审计。
8. **经验沉淀**：将执行结果、复盘内容或经验规则沉淀为可复用能力。

仅完成“输入问题 → 模型生成报告”通常不够，因为可能缺少：

- 实际工具调用；
- 状态协同；
- 结果验证；
- 执行证据；
- 风险控制；
- 经验回流。

---

## 三、MCP、RAG 与可观测性的定位

### 3.1 MCP：推荐可选，但接口契约不能缺失

MCP 本身不是绝对必选项。

如果使用 MCP，应将其定位为**工具连接层**：

- Skill：任务能力抽象层；
- MCP：外部工具和系统连接层。

如果不使用 MCP，则必须提供等价的工具集成契约，包括：

- 协议；
- 鉴权方式；
- 输入输出 Schema；
- 错误处理；
- 审计记录；
- 后续迁移到 MCP 的成本。

还应进一步说明：

- 工具名称；
- 调用入口；
- 参数 Schema；
- 返回结构；
- 权限范围；
- 重试策略；
- 幂等控制；
- 审计日志；
- 降级方式。

评委需要能够判断：将来迁移到 MCP 时，是否只需增加协议适配，而不是重新设计整条工具调用链。

### 3.2 RAG：推荐可选，但上下文能力有硬要求

RAG 不是强制项，但下列四项能力至少实现两项：

1. **Agent 记忆存储**：保存会话历史和决策上下文，支持时间窗口查询和语义检索。
2. **知识库 RAG**：检索规范、Runbook、业务资料和历史案例。
3. **共享状态管理**：管理多 Agent 协同状态，并保证并发一致性。
4. **轨迹可观测**：持久化执行轨迹与证据链，支持回放和审计。

如果明确不使用知识库 RAG，则必须在其余三项中至少实现两项，并说明上下文机制为何有效。

### 3.3 可观测：推荐可选，但直接影响工程评分

可观测不是形式上的必选项，但直接影响“工程落地、运行验证与安全可审计”这一项 20% 的评分。

建议覆盖以下一至两类以上数据：

#### Trace

记录：

- Agent 调用；
- Skill 调用；
- MCP 调用；
- RAG 检索；
- LLM 推理；
- 审批与工具执行链路。

尽量遵循 OpenTelemetry GenAI 语义标准。

#### Log

记录：

- 决策依据；
- 失败原因；
- 权限校验；
- 审批事件；
- 审计信息。

建议通过 Trace ID 与 Trace 关联。

#### Metrics

可包含：

- 任务或会话数量；
- 端到端延迟；
- TTFT；
- Token 消耗；
- Tool 成功率；
- 任务完成率；
- 成本；
- 可靠性指标。

---

## 四、允许使用的技术与披露义务

### 4.1 商业 API

允许使用，但必须披露：

- 调用环节；
- 费用假设；
- 权限范围；
- 可替代方案；
- 潜在供应商锁定；
- 对可复现性的影响。

### 4.2 闭源模型

允许使用，但必须披露：

- 使用范围；
- 使用原因；
- 替代方案；
- 迁移成本；
- 对可复现性的影响。

### 4.3 基于已有项目继续开发

允许，但必须说明：

- 原项目来源；
- 原项目已有功能；
- 本团队贡献范围；
- 新增创新点；
- 与原许可证的兼容关系。

### 4.4 开源范围

不强制所有作品完整开源，但必须清楚说明：

- 开放或开源范围；
- 开源协议或授权方式；
- 第三方依赖；
- 商业 API；
- 闭源模型；
- 数据来源与授权；
- 可复现方式；
- 部署依赖；
- 后续维护计划。

进入复赛和决赛后，必须提交：

- 可运行 AgentTeams 代码；或
- 等价可验证工程材料；
- 评审可访问的仓库或交付包。

### 4.5 企业数据

如果使用日志、工单、知识库、业务数据或个人信息，必须披露：

- 数据来源；
- 授权状态；
- 脱敏方式；
- 合规边界。

---

## 五、团队与报名规则

### 5.1 组队人数

- 可以个人参赛；
- 组队时团队人数不超过 **3 人**。

“至少三个 Agent”不等于至少三名队员；个人也可以实现三个以上 Agent。

### 5.2 成员变更与实名核验

- 初赛队伍提交后，不得变更参赛成员信息；
- 进入复赛后，所有成员需接受实名信息核验；
- 未完成核验或实名信息不实，可能取消后续参赛及获奖资格。

### 5.3 报名信息

参赛团队需提交：

- 报名人姓名；
- 国家或地区；
- 手机号；
- 常用邮箱；
- 队伍名称；
- 团队总人数；
- 团队成员身份及联系方式；
- 意向赛道。

---

## 六、赛程与各阶段交付物

> 手册仅标注月日，没有明确年份；具体年份和临时调整以赛事官网及组委会最终通知为准。

### 6.1 赛事时间表

| 阶段 | 时间 | 关键节点或结果 | 主要提交物 |
|---|---|---|---|
| 报名开启 | 7 月 16 日 | 报名启动 | 报名信息 |
| 初赛 | 7 月 16 日—8 月 16 日 | 8 月 16 日截止 | 作品简介、方案 PPT |
| 初赛评审 | 8 月 17 日—8 月 24 日 | 8 月 24 日公布 Top 30 | 原则上无新增提交 |
| 复赛 | 8 月 25 日—9 月 3 日 | 9 月 3 日截止 | 更新方案、代码包、Demo 或视频 |
| 复赛评审 | 9 月 4 日—9 月 10 日 | 9 月 10 日公布 Top 15 | 原则上无新增提交 |
| 决赛 | 9 月 22 日 | 线下答辩和展示 | 路演 PPT、现场 Demo、最终仓库或工程材料 |
| GOAI DAY / 颁奖 | 9 月 23 日 | 颁奖、项目展示和生态对接 | 路演 PPT、现场 Demo |

### 6.2 初赛

#### 初赛目标

重点评估：

- 项目方向；
- 技术方案；
- 开放或开源价值；
- 可行性。

初赛不强制要求提交可运行代码。

#### 必交一：作品简介

要求：

- 文字说明；
- **500 字以内**；
- 项目名称约 **20 字**。

内容应包括：

- 问题与场景；
- 目标用户；
- 核心方案；
- 使用的系统、模型、Agent 和工具链；
- 创新点；
- 开放与复用价值；
- 当前进展；
- 是否已有 Demo、原型、代码或评测数据。

#### 必交二：方案 PPT

格式为 PPT 或 PDF，建议覆盖：

1. 场景、用户、痛点、价值和行业可复制性；
2. 系统架构、Agent 分工、任务拆解、协作流、上下文和异常分支；
3. 核心 Skill；
4. 工具、云产品、企业系统、MCP、RAG 和可观测规划；
5. 评估指标；
6. Demo 落地计划；
7. 安全边界和风险控制；
8. 开放或开源计划。

#### 可选：AgentTeams 代码包

初赛不强制提交。如果提交，应包含：

- 运行入口；
- 依赖说明；
- 配置文件；
- 样例输入输出；
- 运行证据。

### 6.3 复赛

#### 复赛目标

重点评估：

- 项目完成度；
- Demo 可运行性；
- 工程实现；
- 评测结果；
- 开放或开源规范。

#### 必交一：更新版项目方案

格式为 PPT 或 PDF，必须包含：

- 根据初赛反馈更新后的方案；
- 至少一个可运行、可验证的完整场景；
- 任务触发；
- AgentTeams 编排；
- 多 Agent 协作；
- 工具调用；
- 结果验证；
- 证据沉淀；
- 样例输入输出；
- Log、Trace、Metrics；
- 评测结果；
- 自动化验证证据；
- 核心 Skill 和接口 Schema；
- 数据流和工程结构；
- 失败处理和异常分支；
- 权限、审批、回滚和审计；
- 开源协议和第三方依赖；
- 商业 API、闭源模型和数据授权；
- 维护与落地计划。

#### 必交二：可执行 AgentTeams 代码包

必须包含：

- 运行入口；
- 依赖说明；
- 配置文件；
- 样例输入输出；
- 运行证据。

#### 必交三：可运行 Demo 或 Demo 视频

应展示：

- 至少一个完整场景；
- Agent 协作；
- 工具调用；
- 输出结果；
- 异常处理；
- 执行证据；
- 失败处理；
- 关键技术亮点。

Demo 可以采用：

- 在线体验；
- 本地部署；
- 视频演示。

### 6.4 决赛

#### 必交一：决赛路演 PPT

必须包括：

- 场景来源、目标用户和真实价值；
- 行业普遍性和推广潜力；
- AgentTeams 编排；
- 多 Agent 分工；
- 核心 Skill；
- 工具集成和数据流；
- Demo 结果；
- 日志、Trace、Metrics 和评测结果；
- 异常处理；
- 部署和依赖；
- 权限、审批、回滚、审计和降级；
- 可复现能力；
- 失败案例和风险应对；
- 开源范围、协议、Skill 分发、版本迭代和维护计划。

#### 必交二：现场 Demo

必须：

- 展示完整场景；
- 验证 Agent 协作和工具调用；
- 展示输出、异常处理和执行证据；
- 展示失败处理和关键技术亮点。

展示界面可以采用：

- AgentTeams 自带的 Element 聊天室；
- 自研 Web UI；
- 其他可视化方式。

自研前端不是强制项。

#### 必交三：最终代码仓库或等价工程材料

必须提供评审可访问的最终版本，并包含：

- README；
- 部署说明；
- 开源协议；
- 示例配置；
- 测试方法。

---

## 七、评审标准与权重

| 评审项 | 权重 |
|---|---:|
| 场景价值与行业可复制性 | **25%** |
| 多 Agent 协同与自主闭环能力 | **25%** |
| Skill 工程体系与生态复用 | **25%** |
| 工程落地、运行验证与安全可审计 | **20%** |
| 开放/开源贡献 | **5%** |

### 7.1 场景价值与行业可复制性：25%

评审重点：

- 是否解决真实、明确、具有代表性的问题；
- 用户和痛点是否清楚；
- 是否有可感知收益；
- 是否能够在其他组织、行业或流程中复制。

### 7.2 多 Agent 协同与自主闭环：25%

评审重点：

- Agent 职责是否清晰；
- 任务拆解是否合理；
- 上下文和协作协议是否结构化；
- 状态流转是否明确；
- 能否处理异常、冲突和多方案选择；
- 高风险操作是否支持人工确认、审批、回滚和审计。

### 7.3 Skill 工程体系与复用：25%

评审重点：

- 是否覆盖核心任务能力；
- 输入输出和调用条件是否明确；
- 依赖和失败处理是否完整；
- 是否能跨 Agent、跨场景复用；
- 是否支持版本、发布、回滚和评估；
- 是否以 AgentTeams 为协同基点。

### 7.4 工程落地与安全审计：20%

评审重点：

- Demo 是否能够实际运行；
- 部署配置是否清楚；
- 模型、工具和数据依赖是否透明；
- 是否提供 Log、Trace、Metrics 和运行报告；
- MCP、RAG、可观测链路是否可验证；
- 权限、密钥、审批、回滚、降级和审计是否完整。

### 7.5 开放/开源贡献：5%

评审重点：

- 是否形成可复用成果；
- 是否具有接口契约、文档和示例；
- 协议和第三方依赖是否清楚。

### 7.6 不按工具数量评分

规则特别说明：

> 推荐工具和云产品不按使用数量评分。

堆叠 Nacos、Higress、RocketMQ、PolarDB 等产品不会自动加分。评委关注的是：

- 必要性；
- 接口契约；
- 可替换性；
- 权限与安全边界；
- 端到端闭环证据；
- 后续迁移成本。

---

## 八、红线与淘汰风险

以下行为可能严重扣分或直接取消资格：

1. 作弊；
2. 队伍间私下共享代码；如需共享，应合并队伍；
3. 代码高度雷同；
4. 只提交概念 PPT 或营销材料，没有 PoC、日志、视频、仿真或等价验证证据；
5. 决赛封版后，按照 README 和部署说明无法复现核心 Demo；
6. 决赛展示与复赛承诺严重不符；
7. 临时拼凑非团队贡献；
8. 未披露已有项目基础；
9. 抄袭、套壳；
10. 伪造实验结果；
11. 刷社区数据；
12. 购买虚假 Star、Fork、Issue 或 PR；
13. 对数据、许可证、商业 API、闭源模型、个人信息、企业数据或安全边界披露不实。

---

## 九、推荐工具链

### 必选

- **AgentTeams（原名 Hiclaw）**。

### 推荐

- 阿里云官方用云 Skills；
- Nacos；
- Higress；
- PolarDB for PostgreSQL；
- RocketMQ；
- LoongSuite；
- AgentScope Studio；
- 阿里云 AgentLoop。

允许使用替代方案，但必须说明：

- 接口兼容性；
- 替换原因；
- 迁移成本。

---

## 十、符合赛题要求的参考架构

```text
真实企业任务输入
        │
        ▼
AgentTeams 协同编排层
  ├─ 角色编排
  ├─ 任务拆解
  ├─ 上下文传递
  ├─ 状态追踪
  └─ 异常与升级策略
        │
        ▼
至少 3 个不同职能 Agent
        │
        ▼
可复用 Skill 能力层
        │
        ▼
MCP 或等价工具契约层
        │
        ▼
真实工具 / 企业系统 / 云产品
        │
        ▼
结果验证 + Trace / Log / Metrics
        │
        ▼
审批 / 回滚 / 审计 / 经验沉淀
```

判断项目框架是否符合要求的关键不是使用了什么 Web 框架或 LLM SDK，而是：

> **AgentTeams 是否处于协同系统核心，而非外围适配器；Skill 是否形成稳定能力边界；系统是否形成可验证、可审计、可回滚的真实任务闭环。**

---

## 十一、提交前核对清单

### 架构

- [ ] 至少有 3 个不同职能 Agent。
- [ ] 每个 Agent 都有完整 Identity 定义。
- [ ] AgentTeams 是核心协同设计与执行基点。
- [ ] 角色编排、任务拆解、上下文传递、协同执行、状态追踪均可验证。
- [ ] 系统形成端到端任务闭环。

### Skill 与工具

- [ ] Skill 不是一次性脚本或 Prompt 描述。
- [ ] 已提供核心 Skill 清单。
- [ ] 每个 Skill 都有输入、输出、调用条件、依赖、失败处理和安全边界。
- [ ] 使用 MCP，或提供等价工具集成契约。
- [ ] 外部工具具有鉴权、Schema、重试、幂等、审计和降级设计。

### 上下文与证据

- [ ] 四项上下文能力至少实现两项。
- [ ] 有共享状态或 Agent 记忆机制。
- [ ] 有 Trace、Log 或 Metrics。
- [ ] 有样例输入输出和可回放执行证据。
- [ ] 有结果验证，而不只是模型给出结论。

### 安全与合规

- [ ] 高风险动作需要人工确认或审批。
- [ ] 支持回滚、降级和审计。
- [ ] 已披露商业 API 和闭源模型。
- [ ] 已披露第三方依赖及版本。
- [ ] 已说明数据来源、授权和脱敏方式。
- [ ] 已说明开源范围、协议和可复现方式。

### 复赛及决赛工程材料

- [ ] 提供可访问仓库或交付包。
- [ ] README 能让评委复现核心 Demo。
- [ ] 包含运行入口、依赖安装、配置方法和环境要求。
- [ ] 包含示例配置和测试方法。
- [ ] Demo 展示正常链路、异常分支、失败处理和执行证据。
